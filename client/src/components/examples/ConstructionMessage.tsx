import ConstructionMessage from '../ConstructionMessage';

export default function ConstructionMessageExample() {
  return (
    <div className="p-8 bg-background min-h-screen flex items-center justify-center">
      <ConstructionMessage />
    </div>
  );
}